import { LocationServiceMock } from './location-service-mock';

describe('LocationServiceMock', () => {
  it('should create an instance', () => {
    expect(new LocationServiceMock()).toBeTruthy();
  });
});
